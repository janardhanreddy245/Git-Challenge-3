name: Deploy Flask application

on:
  push:
    branches:
      - main

jobs:
  dockerbuild:
    runs-on: ubuntu-latest  # This indicates using the self-hosted runner

    steps:          
      - name: Code Checkout
        uses: actions/checkout@v4 
      # Step 4: Run tests (optional but recommended)
      - name: Docker build
        run: docker build -t dockerimage:v1.0 .
        

          
          
